#include <stdio.h>
int main(){
	// logical operators
	
	// and &&
    // or ||
    // not !
    
    // true 1
    // false 0
    
    printf("%d \n",(20 < 40 && 45 == 53));
    printf("%d \n",(20 < 40 || 45 == 50));
	printf("%d \n",(30 != 40));
	return 0;
	
    
}
